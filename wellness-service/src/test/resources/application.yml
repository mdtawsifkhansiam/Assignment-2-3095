spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/eventdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: none
  data:
    redis:
      host: localhost
      port: 6379